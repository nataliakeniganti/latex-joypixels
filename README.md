### Prerequisites
To compile the LaTeX files in this repo, you need the `joypixels` package:
1. Ensure you have **XeLaTeX** or **LuaLaTeX**.
2. Install the package: `tlmgr install joypixels`
3. Use in your preamble:
   ```latex
   \usepackage{joypixels}
